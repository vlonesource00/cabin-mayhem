[package]
name = "cabin-mayhem"
version = "0.1.0"
description = "Cabin Mayhem desktop shell"
authors = ["Cabin Mayhem"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
