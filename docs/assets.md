# Asset Catalog

One project-owned source SVG provides desktop icon variants. Cabin visuals are original runtime Canvas geometry in `src/app/cabin-mayhem-app.ts`; no external art, music, characters, airline branding or game layouts are used.

Before adding assets, record source, license, owner, import transformation and every use in `public/assets/manifest.json`.
